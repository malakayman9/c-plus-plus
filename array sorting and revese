#include<iostream>
#include <algorithm>
using namespace std;
void main() {

	const int size = 5;
	int x[size];
	int a, b;
	for (int a = 0; a < size; a++)
	{
		cin >> x[a];
	}
sort(x, x+size);
	reverse(x, x + size);
	for (int i = 0; i< size; i++)
	{
		cout << x[i]<< endl ;
	}
}
