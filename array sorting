#include<iostream>
using namespace std;
void main() {

	const int size = 5;
	int x[size];
	int a, b;
	for (int a = 0; a < size; a++)
	{
		cin >> x[a];
	}
for (int a = 0; a < size; a++)
	{
		for (int b = a + 1; b < size; b++)
		{
			int temp;
			if (x[a] > x[b])
			{

				temp = x[a];
				x[a] = x[b];
				x[b] = temp;


			}
		}
	}
  for (int i = 0; i< size; i++)
	{
		cout << x[i]<< endl ;
	}
  }
