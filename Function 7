#include<iostream>
using namespace std;
void search(string x[], int y,string name)
{
	bool z;
	for (int i = 0; i < y; i++)
	{
		if (x[i] == name) 
		{
			z = true;
		}
		
	}
	if (z == true) 
	{
		cout<<"found";
	}
	else {
		cout << "not found";
	}
}
void main() 
{
	const int size = 5;
	string name;
	
	string x[size];
	for (int i = 0; i < size; i++)
	{
		cin >> x[i];
	}
    cout << "enter name "<< endl;
	cin >> name;
	search(x, size, name);

}
