#include<iostream>
using namespace std;
class calculation
{
public:
	int sum(int x, int y)
	{
		
		return x + y;
	}
	void sum(float x, int y, int z)
	{
		cout << x + y + z;
	}
	float sum(int x, int y, int z)
	{
		return x + y + z;
	}
};
void main()
{
	calculation c1;
	c1.sum(2.1f, 5, 4);
}
